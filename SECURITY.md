# Security Policy

Do not report leaked credentials in a public issue. Revoke the affected
Feishu/Lark App Secret or token first, then contact the repository owner through
GitHub privately.

Before sharing diagnostics, remove `config.env`, `lark-home/`, session JSON,
worker logs, Open IDs, chat IDs, message IDs, and local filesystem paths.
